---
title: Coding Journal — July 21, 2026
date: July 21, 2026
---

## Wins

Locked the system map for Open Brain: one Postgres on the miss-minutes VM, agents as MCP clients — claude.ai over HTTPS brain.ofone.dev (capture+search), Hermes on stdio with the full tool set. Pushed a production-state README to `github.com/lazo99/open-brain-mcp` so the public repo actually matches how this runs, and synced the xAI key into Bitwarden from the GCP SM secret so I’m not hunting it across machines.

Also did the unglamorous but necessary work before the laptop wipe: full restore checklist (SSH/GCP keys, secrets, OpenHuman state tarball, Claude Code config via dotfiles, reclones). Force-pushed `harness` and `miss-minutes-mcp` with laptop as source of truth after the name collisions; old remote content is on `backup-laptop-2026-07-21` if I ever need it. Stack preference for Miss Minutes is settling too — Grok 4.5 + Grok Build direct xAI, Hermes desktop first on the fresh laptop, Claude Pro as the safety net.

## Losses

OC-catalog Grok keys are still invalid; raw xAI works, so that’s the path for now. The big reinstall gap: `~/reinstall-backup/db-extracts/` (friday-brain notes, discord-cp, ai-harness memories, openclaw chunks, rag-store embeddings) was local-only and never left the machine — if any of that mattered, it’s gone. Ops cost is still winning over pure $/1M math. Friday server/flutter client is still in limbo (archive or not) and didn’t get decided before the wipe.
